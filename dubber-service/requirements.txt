# Dubber service (Phase 0) — runtime subset of the autodubber pipeline.
# Excludes faster-whisper (replaced by Deepgram) and demucs/torch (BGM, GPU).

fastapi
uvicorn[standard]
pydantic
requests

# Pipeline dependencies (reused from the dubber/ package)
yt-dlp
edge-tts
pydub
deep-translator
google-genai
numpy
soundfile
httpx
python-dotenv

# System requirement (not pip): FFmpeg + FFprobe must be on PATH.
boto3
# >=4.8 for the FaceDetectorYN (YuNet) / FaceRecognizerSF (SFace) re-ID APIs and
# the 2023mar YuNet model used by dubber/shorts_reframe.py.
opencv-python-headless>=4.8
Pillow
gdown
